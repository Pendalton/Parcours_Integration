@model IEnumerable<Parcours_integration.Models.Secteurs>
@{
    ViewBag.Title = "Trombinoscope";
}

<h2>Trombinoscope des employés</h2>
<hr />


<div class="row">
    <div class="col-md-9">
        Cette page affichera les employés de l'usine triés par hiérarchie, ainsi que leur postes et leur photo.
        <br />
        <br />
        Trier selon les secteurs permettra de n'afficher que les employés de ce secteurs pour faciliter l'identification des employés par les nouveaux arrivants.
    </div>
    <div class="col-md-3">
        <ul id="treeview-search" class="treeview" style="list-style:none; margin-left:-20px">
            @foreach (var type in Model)
            {
                <li class="list-group-item">
                    <a href="#" style="text-decoration: none; font-weight: bold">@type.Nom</a>
                </li>
            }
        </ul>
    </div>
</div>